export const TABLES = {
  USERS: "users"
} as const;

export const ROLES = {
  ADMIN: "Admin",
  USER: "User"
} as const;

export const USER_STATUS = {
  ACTIVE: "Active",
  DELETED: "Deleted"
} as const;
